extends layout

block content

  h1= title
  p Welcome to #{title}
 
  a(href="/new") 
    button.btnNew New
   .card-cont
    - for (let i=0 ; i < messages.length; i++){
      .card
        div.text #{messages[i].text}
        div.user #{messages[i].user}
        div.date #{messages[i].added}
    -  }
  

 
  